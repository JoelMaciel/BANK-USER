server:
  port: 8087
  servlet:
    context-path: '/bank-user/'

spring:
  application:
    name: bank-user-service
  datasource:
    url: jdbc:mysql://localhost/bank_user?createDatabaseIfNotExist=true&serverTimeZone=UTC
    username: root
    password:
  flyway:
    locations: classpath:db/migration


  jpa:
    hibernate:
      ddl-auto: none
      properties: org.hibernate.dialect.MySQL8Dialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true
  rabbitmq:
    addresses: amqps://crdzywry:s60VjJGfFG8XvLV47DGyCp5Ig8l2oyCi@woodpecker.rmq.cloudamqp.com/crdzywry

logging:
  level:
    com.ead: TRACE
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate: INFO

bank:
  api:
    url:
      account: 'http://bank-account-service/bank-account'
  broker:
    exchange:
      userEvent: bank.userevent

eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:8761/eureka'
  instance:
    hostname: localhost